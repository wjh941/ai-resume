# Local development uses Vite's proxy to http://127.0.0.1:8000 when this is empty.
# For a deployed browser frontend, set the public HTTPS API origin.
# This value is compiled into the browser. Do not place tokens, API keys, or secrets here.
VITE_API_BASE_URL=
