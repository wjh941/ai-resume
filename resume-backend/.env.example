# Runtime
APP_ENV=development
APP_HOST=127.0.0.1
APP_PORT=8000
DATABASE_PATH=./data/resume_demo.db
TEMP_FILE_PATH=./temp
EXPORT_FILE_EXPIRE_MINUTES=60
PDF_RENDERER=playwright
PLAYWRIGHT_BROWSERS_PATH=D:/Projects/ai-resume-miniprogram/.cache/playwright

# JWT authentication (一期 SQLite 过渡；二期 MySQL/PostgreSQL 保持相同 Token 声明)
# 生产环境必须替换为至少 32 字节的随机密钥，且不得提交真实值。
JWT_SECRET=replace-with-a-long-random-production-secret
JWT_EXPIRE_HOURS=24

# Login transition controls
# true 仅用于本地演示，验证码固定为 123456；生产必须设置为 false。
AUTH_DEMO_MODE=true
# 后续真实短信接入选择 aliyun、tencent 或 http；本期不发送任何真实短信。
SMS_PROVIDER=disabled
SMS_ALIYUN_ACCESS_KEY_ID=
SMS_ALIYUN_ACCESS_KEY_SECRET=
SMS_TENCENT_SECRET_ID=
SMS_TENCENT_SECRET_KEY=
SMS_HTTP_ENDPOINT=
SMS_HTTP_TOKEN=
# 微信扫码占位。提供 AppID、AppSecret、HTTPS 回调域名后再实现 OAuth 回调。
WECHAT_OPEN_APP_ID=
WECHAT_OPEN_APP_SECRET=
WECHAT_OPEN_REDIRECT_URI=

# Real cloud AI: supports Ark and OpenAI-compatible Chat Completions.
# 未配置 KEY 或 MODEL 时后端返回 ai_not_configured，前端仅使用临时内存兜底。
AI_PROVIDER=openai_compatible
AI_BASE_URL=https://ark.cn-beijing.volces.com/api/v1
AI_API_KEY=
AI_MODEL=

# Browser and Mini Program development origin allowlist (comma-separated exact origins).
CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# 一期文件仍保存在 TEMP_FILE_PATH。二期可在导出服务接入 OSS/COS，不应改变 JWT 所有权校验。
OBJECT_STORAGE_PROVIDER=local
OSS_ENDPOINT=
OSS_BUCKET=
OSS_ACCESS_KEY_ID=
OSS_ACCESS_KEY_SECRET=
COS_SECRET_ID=
COS_SECRET_KEY=
COS_BUCKET=
COS_REGION=

# Existing local search configuration
CACHE_EXPIRE_DAY=7
WEB_SEARCH_PROVIDER=disabled
TAVILY_API_KEY=
WEB_SEARCH_BASE_URL=https://api.tavily.com
WEB_SEARCH_MAX_RESULTS=5
